module.exports = {
    GETH: {
        SERVER_ADDRESS : "localhost",
        SERVER_PORT : "8545",
        NETWORK_ID : "network_id",
        ORIGIN_ADDR : "address",
        ORIGIN_ADDR_PASS : "password"
    },
    JWT: {
        SECRET_KEY : "secret key"
    },
    HTTP: {
        PORT : 80
    }
}